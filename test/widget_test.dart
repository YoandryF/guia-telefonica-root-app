// Tests de smoke — verifican que la app arranca sin crash
// Los tests reales están en test/repositories/ y test/models/

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — ver test/models/ y test/repositories/ para tests reales', () {
    expect(true, isTrue);
  });
}
